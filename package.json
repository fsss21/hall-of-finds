{
  "name": "hall-of-finds",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:server": "pkg src/server/index.js --out-path build --targets node18-win-x64,node18-macos-x64 --assets build",
    "build:server:win": "pkg src/server/index.js --out-path build --targets node18-win-x64 --assets build",
    "build:server:mac": "pkg src/server/index.js --out-path build --targets node18-macos-x64 --assets build",
    "build:all": "npm run build && npm run build:server",
    "build:win": "npm run build && npm run build:server:win",
    "build:mac": "npm run build && npm run build:server:mac",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@mui/icons-material": "^7.3.7",
    "@mui/material": "^7.3.7",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "fs-extra": "^11.3.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "eslint": "^8.55.0",
    "eslint-plugin-react": "^7.33.2",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.5",
    "pkg": "^5.8.1",
    "vite": "^5.0.0"
  },
  "pkg": {
    "scripts": [
      "src/server/index.js"
    ],
    "assets": [
      "build/**/*"
    ],
    "targets": [
      "node18-win-x64",
      "node18-macos-x64"
    ],
    "outputPath": "build"
  }
}
